{
  "$schema": "https://schema.railpack.com",
  "phpExtensions": ["gd"],
  "steps": {
    "install:composer": {
      "variables": {
        "COMPOSER_FUND": "0",
        "COMPOSER_CACHE_DIR": "/opt/cache/composer",
        "COMPOSER_MAX_PARALLEL_HTTP": "4"
      },
      "commands": [
        {
          "customName": "composer install (retries, then git source)",
          "cmd": "sh -c 'composer install --optimize-autoloader --no-scripts --no-interaction || { echo retrying in 5s; sleep 5; composer install --optimize-autoloader --no-scripts --no-interaction; } || { echo retrying in 15s; sleep 15; composer install --optimize-autoloader --no-scripts --no-interaction; } || { echo dist downloads keep failing - falling back to git source; sleep 15; composer install --optimize-autoloader --no-scripts --no-interaction --prefer-source; }'"
        }
      ]
    }
  }
}
